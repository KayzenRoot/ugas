# UGAS consumer examples

These folders are intentionally not complete games. They are fixtures for the installer and a readable demonstration of the local `.game-assets/` contract. Rebuild their metadata with `python scripts/examples/bootstrap_examples.py --force`.
