# UGAS v0.16.1 — MULTI_DIRECTION_ANIMATION_RUNTIME integrity correction

The active phase is `MULTI_DIRECTION_ANIMATION_RUNTIME` on branch `codex/v0.16.0-multi-direction-runtime-foundation`, based on merged v0.15.1 main at `514a17818469b567966293db808cafbf708f8311`. The active gate is `MULTI_DIRECTION_ANIMATION_RUNTIME_INTEGRITY_TECHNICALLY_QUALIFIED`. The active repository is `KayzenRoot/ugas`; PR #6 has `pr_number=6`, `pr_state=OPEN`, and `real_pr_checks_green=true` only for the corrected exact head after the three required Actions contexts complete successfully.

This is a correction-only slice. The v0.16.0 reviewed head `7d1e999e91ee8817c6754b363a5c19f1ba6f2e7d` is preserved as `CORRECTION_REQUIRED` historical evidence under `docs/evidence/multi-direction-runtime-v0160/`. The correction replaces copied positive booleans with real DIR-NC-01 through DIR-NC-12 runtime mutations and observed rejection records, makes invalid vectors `INVALID_VECTOR_UNRESOLVED` even when retained facing is present, binds the active state to PR #6, and proves that test-only exact resolution is never `production_safe`.

The canonical directions are `south`, `south_east`, `east`, `north_east`, `north`, `north_west`, `west`, and `south_west`. Screen space is +x east/right and +y south/down, with deterministic 45-degree sectors and explicit aliases. The production registry contains only the six approved front profiles at `south`; `front` remains a compatibility alias for `south`. No fabricated directional artwork is claimed. `TEST_ONLY_SYNTHETIC_DIRECTION_FIXTURE` remains outside production registries and is carried forward byte-identically by hash for QA only.

Evidence is under `docs/evidence/multi-direction-runtime-v0161/`: the contract, south-only coverage manifest, invalid-vector QA, test-only production-safety QA, real negative controls, fallback/mirror/cache/provenance evidence, state consistency, validation totals, and the carried-forward synthetic contact sheet. Existing v0.15.1 front profile bytes and the frozen v0.16.0 review history are preserved.

Required exact GitHub contexts are `UGAS CI / unit-and-validation`, `UGAS CI / docker-smoke`, and `UGAS Review / evidence`. The artifact is uploaded before final enforcement and is bound to the exact PR #6 head. Local unit/official validation are evidence for their own gates; exact-head GitHub Actions success is required for the review record. The PR remains OPEN and unmerged.

The production boundary remains `production_approved=false`, `production_routing=BLOCKED`, and `new_generation=0`. No SAM2, ComfyUI, diffusion, equipment, outfits, creatures, items, environment, UI asset family, or VFX asset family work is authorized here. Merge only after external review. Stop with PR #6 OPEN after the corrected exact head has all three Actions contexts green and the v0.16.1 artifact exists; hand off the next decision to Sol.
