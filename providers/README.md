# Provider contracts

Provider manifests are capability declarations, not live health results. Runtime probes are intentionally lightweight and never persist tokens. The default architecture is ComfyUI-first for local visual generation, with an RTX 5050 Render Node over a private network and Hugging Face as an explicit alternative.
