# UGAS v0.19.1 review boundary

The direct user request accompanying this attachment is empty. The attached PDF is therefore the governing executable prompt. Its instructions are kept separate from that empty request and are executed in order, with v0.19.0 rejection history preserved as immutable evidence.

The active phase is `ITEMS_PROPS`. The v0.19.0 candidate at reviewed head `44937935e644202836b3e1f081b6a63201b850db` on PR #9 was rejected and remains frozen under `docs/evidence/items-props-runtime-v0190/`. This v0.19.1 correction remains forward-only on the existing branch `codex/v0.19.0-items-props-runtime-foundation`, based on merged main `52938a04016352d50ad54621a4df981a9c36b058`; it does not create a duplicate PR.

The technical gate is `ITEMS_PROPS_LINKAGE_REPRESENTATION_STACK_INTEGRITY_TECHNICALLY_QUALIFIED`. Sol's forward-only decision is `APPROVED_FOUNDATION / APPROVED_TO_MERGE` for the exact reviewed head `00905ed17a8a95540cb496142b207778c66ba8ac`, with the bounded artifact and 3/3 exact-head checks recorded in `docs/evidence/github-governance-v0200/v0191-external-approval.json`. The six canonical classes remain `weapon_item`, `consumable_item`, `material_item`, `quest_key_item`, `container_prop`, and `environmental_prop`, with explicit inventory, world, and equipment-preview representation contexts. Every non-UNSUPPORTED binding now carries an artifact path, byte size, dimensions, file SHA-256, decoded-pixel SHA-256, representation identity/revision, and provenance; the resolver rejects absent or mismatched bytes and never resolves an unbound representation.

Equipment linkage is identity-only and authority-backed. The positive `iron_sword` preview resolves the existing v0.17.1 TEST_ONLY asset `fixture-charm-violet` / `accessory` / `violet` / `fixture-charm-violet-r1`, and exposes authority version, SHA-256, blob id, and resolved identity. LK-NC-01..05 cover missing id, slot, variant, revision, and test/production boundary mismatches through the same authority path. The v0.17.1 equipment compositor is not duplicated.

Stackable records use item-family identities (`item:healing_potion` and `item:moon_ore`); generic class keys, unauthorized cross-item family collisions, and silent derived-family changes reject closed. Derived variants are materialized and revalidated before resolution. Cache identity includes the item, class, variant/revision, context, representation and byte identity, stack family, equipment identity, authority hash, and registry mode.

The complete TEST_ONLY determinism fixture is generated twice in independent temporary directories and compares all 19 generated files: 13 representation files for all six items and declared contexts, manifest/byte manifest, contact sheet, geometry sheet, identity JSON, and lineage JSON. The evidence records `file_count` and a complete `differences` array; mutating one world representation and one identity field in the second run is rejected.

The bounded evidence is under `docs/evidence/items-props-runtime-v0191/`. It includes the v0.19.1 manifest, linkage, representation-byte manifest, stack identity, derived state, cache/provenance QA, all 25 negative controls (`IP-NC-01..17`, `LK-NC-01..05`, `ST-NC-01..03`), production boundary, determinism, state consistency, and execution records, plus contact and geometry sheets rebuilt from bound TEST_ONLY bytes.

`real_item_prop_asset_coverage=NONE`, `synthetic_item_prop_fixture=TEST_ONLY`, `production_approved=false`, `production_routing=BLOCKED`, and `new_generation=0` remain binding. The production registry is empty. The v0.19.1 runtime/evidence and its representation bytes are closed and immutable; environment/tilesets is `NEXT NECESSARY` only after PR #9's bookkeeping-only reproof and governed merge. Real item/prop generation, production routing, maps/minimap, UI, VFX, orchestration hardening, and other new capability remain outside this release.

The expected pre-merge state is `version=0.19.1`, `phase=ITEMS_PROPS`, `current_gate=ITEMS_PROPS_LINKAGE_REPRESENTATION_STACK_INTEGRITY_TECHNICALLY_QUALIFIED`, `creatures_monsters=APPROVED_FOUNDATION`, `items_props=APPROVED_FOUNDATION`, `items_props_runtime_external_review=APPROVED_FOUNDATION`, `v0.19.0_external_review=CORRECTION_REQUIRED`, `allowed_next_actions=[governed_merge_pr_9]`, and `next_capability_started=false`. PR #9 must remain OPEN until the protected governed merge succeeds; after that merge the v0.20.0 Environment/Tilesets prompt may start on a new branch. The prior `external_review_items_props_v0191` state is preserved as the pre-approval checkpoint.

`GITHUB_OPERATIONS_AUTOMATION_POLICY=ENABLED`, `GITHUB_REVIEW_MODE=PR_FIRST`, `NO_SELF_MERGE_UNTIL_EXTERNAL_VISUAL_APPROVAL=true`, `ALWAYS_ON_DASHBOARD_POLICY=ENABLED`, and `DOCKER_ALWAYS_ON_LOCAL` remain binding.
