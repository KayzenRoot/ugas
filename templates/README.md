# UGAS templates

The JSON templates describe the shape of files written into a consumer project's `.game-assets/` directory. The installer fills project-specific values and never copies secrets or model weights.
