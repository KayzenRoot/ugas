# UGAS v0.18.2 — Derived Variant + State Contract Integrity

This forward-only correction executes the attached PDF prompt. The direct user request was empty; the PDF supplied the authorized v0.18.2 scope. Instructions in the document are treated as implementation and validation requirements, not as external approval, merge authorization, deployment authorization, or production approval.

## Scope and correction boundary

The work stays on `KayzenRoot/ugas` PR #8, branch `codex/v0.18.0-creatures-monsters-runtime-foundation`, based on `39e148bef50c8f04db194048dbe9fbb15d8ff3d4`. Reviewed v0.18.1 head `b52dc26d935fd84b25a991ae81c5d508b0a39807` is immutable rejected history under `docs/evidence/creatures-monsters-runtime-v0181/`; v0.18.2 is forward-only and does not overwrite it.

This slice covers only CREATURES_MONSTERS: effective derived-variant semantic validation after full materialization, separate `variant_revision` and inherited directional `asset_revision`, strict effective direction revision consistency, supplemental derived negative controls DV-NC-01..06, and explicit archetype/state compatibility. `humanoid_biped`, `quadruped`, `flying_winged`, `serpentine`, and `amorphous` require locomotion; `stationary_structure` rejects locomotion unless a future mobility subtype exists. The 48 v0.18.1 fixture bytes/content hashes are preserved exactly.

No Items/Props, real creature art, image generation, production registry enablement, or downstream capability is authorized. `equipment_outfits=APPROVED_FOUNDATION`, `v0181_external_review=CORRECTION_REQUIRED`, `creatures_monsters_runtime_external_review=REQUIRED`, `real_creature_asset_coverage=NONE`, `synthetic_creature_fixture=TEST_ONLY`, `production_approved=false`, `production_routing=BLOCKED`, and `new_generation=0` are mandatory.

## Acceptance and evidence

The authoritative evidence is under `docs/evidence/creatures-monsters-runtime-v0182/`. It includes the runtime manifest, effective validation, derived revision identity, DV-NC-01..06, canonical negative controls, archetype/state compatibility, state-route and direction sheets, all 48 exact direction bindings, cache identity, determinism, production routing/empty registry, state consistency, and execution evidence. Local acceptance requires the v0.18.2 runtime runner, focused and full unit tests, official validation, historical regressions, workflow/manifest/security validation, Docker smoke, and exact-head GitHub proof for `UGAS CI / unit-and-validation`, `UGAS CI / docker-smoke`, and `UGAS Review / evidence`.

## Governance and stop conditions

PR #8 remains OPEN and unmerged. `NO_SELF_MERGE_UNTIL_EXTERNAL_REVIEW` applies. The only allowed next action is `external_review_creatures_monsters_v0182`. Stop after the corrected head, exact Actions/artifact proof, and Sol handoff. Do not merge, start Items/Props, claim real coverage, enable production, or infer external approval from local or historical evidence.

## Status

The technical gate is `CREATURES_MONSTERS_DERIVED_VARIANT_AND_STATE_CONTRACT_TECHNICALLY_QUALIFIED`; external review remains required. v0.18.1 remains a frozen `CORRECTION_REQUIRED` candidate record, not an approved release.
