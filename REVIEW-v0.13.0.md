# UGAS v0.13.0 — RUN_FRONT_V1 review

## Scope and authorization

This is the active UGAS-v0.13.0-RUN-FRONT-V1 slice. PR #2 was merged at the exact approved head and merge commit 0beb4c23604f1e45736c3082f99d2e08fa1ac308; that v0.12.4 completion is preserved in docs/evidence/current-state-v0.12.4.json. The canonical capability matrix still has CAPABILITY_COUNT=16, next_candidate=RUN_FRONT_V1, production_routing=BLOCKED, and new_generation=0.

The current gate is CUTOUT_ANIMATION_RUNTIME_V1_RUN_FRONT_TECHNICALLY_QUALIFIED. The only allowed next action is external_visual_review_run_front_v1; merge PR #2 is historical completion of the prerequisite v0.12.4 governance recovery, not authorization to merge this PR.

The only executed capability is RUN_FRONT_V1: one front-facing, eight-frame, 12 fps, looping run cycle over the approved immutable R4 cutout source. The adapter is ugas.animation_profiles.run_front_v1; the declarative profile is profiles/animation/run-front-v1.json; the contract is docs/evidence/animation-runtime-v0130/run-front-contract-v0130.json.

GITHUB_OPERATIONS_AUTOMATION_POLICY=ENABLED, USER_MANUAL_GITHUB_OPERATIONS=FALLBACK_ONLY, GITHUB_REVIEW_MODE=PR_FIRST, NO_SELF_MERGE_UNTIL_EXTERNAL_VISUAL_APPROVAL=true, ALWAYS_ON_DASHBOARD_POLICY=ENABLED, and DOCKER_ALWAYS_ON_LOCAL are authoritative. Production remains production_approved=false, production_routing=BLOCKED. The current result is technical only: external_visual=REQUIRED.

## Deterministic implementation

The runtime samples all twelve scalar motion tracks before rendering. It reuses the canonical R4 source, source skeleton, masks, z-order and source pixels; it does not run SAM2, ComfyUI, diffusion, ControlNet, IP-Adapter, pixel interpolation or manual pixel edits. new_generation=0.

The phase contract is R0-contact-left, R1-support-left, R2-passing-left, R3-flight-left, R4-contact-right, R5-support-right, R6-passing-right, R7-flight-right. Contact frames are 0 and 4, passing frames are 2 and 6, flight frames are 3 and 7, and the loop edge is 7→0. Root motion, torso/root participation, arm-leg opposition, source-alpha sole anchoring, constant occlusion plan, finite targets, angular continuity and loop closure are all explicit fail-closed gates with declarative thresholds.

The eight independent negative controls mutate real targets, tracks, dependencies or package QA data and must reject the intended gate: break_loop, zero_root_and_torso, foot_slide, duplicate_leg_phase, invert_arm_opposition, angular_jump, remove_dependency_hash, and false_gate_in_package.

## Evidence and validation

The complete local evidence is under docs/evidence/animation-runtime-v0130/. The reviewable outputs are the eight RGBA frames and manifest, run-front-spritesheet-v0130.png, run-front-preview-v0130.gif, run-front-phase-markers-v0130.png, per-gate QA JSON, execution evidence and negative-control evidence. Hashes bind source, rig, tracks, event markers, targets, frames, package and visual manifest.

Required local gates are schema/track validity, loop closure, cadence and phase alternation, foot-ground/contact, body/root, arm-leg opposition, continuity/interpolation, package integrity, approved-assets-untouched, compile, unit, official validation, no-git snapshot, security/evidence scan and Docker smoke. The official GitHub proof must be generated by the PR at the exact final head and must contain successful UGAS CI / unit-and-validation, UGAS CI / docker-smoke, and UGAS Review / evidence contexts.

## Handoff gate

The branch is codex/v0.13.0-run-front-v1, based on post-merge main 0beb4c23604f1e45736c3082f99d2e08fa1ac308. The handoff creates a PR to main, waits for Actions at the exact pushed SHA, and leaves the PR OPEN. The review artifact must bind the exact head, the three checks, the evidence manifest and the GIF. No merge, production enablement or next capability is authorized.

Final status vocabulary is deliberately narrow: TECHNICALLY_QUALIFIED means local gates passed; external_visual=REQUIRED means no external visual approval is claimed; production_routing=BLOCKED; new_generation=0; next_capability=NOT_STARTED.
